name:{
  "name": "app",
  "description": "sample project using npm",
  "authors": "",
  "license": "ISC",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1" ,
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "all": "npm run build && npm run format && npm run lint"
  },
  "dependencies": {}
}
